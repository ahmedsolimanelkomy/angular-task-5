import { ProductsdirectiveDirective } from './productsdirective.directive';

describe('ProductsdirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ProductsdirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
